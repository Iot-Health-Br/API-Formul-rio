package com.SpringAPIFormulario.service;

import com.SpringAPIFormulario.entity.User;

public interface UserService {
    User saveUser(User user);
    // Você pode adicionar mais métodos, como findUserById, deleteUser, etc.
}
